using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerSwitch : MonoBehaviour
{

  /*  public GameObject Third Person Player;
    // Start is called before the first frame update
    void Start()
    {

    }
       
        Third Person Player.SetActive(true); //set the object to active
        Third Person Player.SetActive(false);//set the object to disable

        gameObject.SetActive(true);//change the state of the current gameobject to active
        gameObject.SetActive(false);//change the state of the current gameobject to disable
      
    // Update is called once per frame
    void Update()
    {
        if (Input.GetButtonDown("Fire3"))
        {
            Third Person Player.SetActive(false);//set the object to disable
            EnemyGoomba.SetActive(true); //set the object to active
        }
        if (Input.GetButtonDown("Jump") && isGrounded)
        {
            velocity.y = Mathf.Sqrt(jumpHeight * -2f * gravity);
        }
    }
  */
}
